-i https://pypi.org/simple

# This repo previously declared NO dependencies at all while every script in
# python/ did `import sportsdataverse` -- it ran entirely on whatever the
# droplet happened to have installed. That is how espn_mbb_06 sat dead for two
# sportsdataverse-py release cycles (see scripts/daily_mbb_scraper.sh).

# Tracks sdv-py main: the stages import sportsdataverse.scrape.espn (the shared
# ESPN raw engine), which is on main and not yet in a PyPI release.
#
# A bare `>=` floor is not a pin on a host with a persistent site-packages --
# pip does not upgrade an already-satisfied requirement. Always install with
# --upgrade:  pip install --upgrade -r requirements.txt
sportsdataverse[all] @ git+https://github.com/sportsdataverse/sportsdataverse-py@main

numpy>=1.19.4
pandas>=1.1.4
pyarrow>=11.0.0
pyreadr>=0.4.7
tqdm>=4.65.0

# argparse and pathlib are deliberately NOT listed. Both are stdlib since
# Python 3.4; the PyPI packages of those names are abandoned Python-2 backports
# and installing `pathlib` shadows the stdlib module. The sibling
# hoopR-nba-raw's requirements.txt still carries both -- do not copy it.
